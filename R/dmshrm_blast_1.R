#' BLAST results obtained from bio3d::blast.pdb().
#' blast.pdb() only queries the Protein Databank (PDB), which only contains high-question sequences.
#' In contrast, compbio4all::blastr() is a modification of blast.pdb() which  defaults instead to RefSeq.

"dmshrm_blast_1"
