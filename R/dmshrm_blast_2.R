#' BLAST results obtained with the blastr() function
#'
#' blastr() is modified from  bio3d::blast.pdb().  blast.pdb() only quries the Protein Databank (PDB), which only contains high-question sequences.  blastr() defaults instead to RefSeq.

"dmshrm_blast_2"
